{
  "demo_url": "https://olist-customer-intelligence-zwtfxo5cpowpdzk8ycu4ju.streamlit.app",
  "dbt_docs_url": "https://sardorbek.codes/olist-customer-intelligence/",
  "verified": "2026-09-16",
  "provenance": "Set a URL ONLY after it is live and has been opened in a browser, and set `verified` to the date it was checked. README.md previously read 'Live at the URL above' with no URL above it -- a claim with nothing behind it, which this project treats as worse than an absent badge. demo_url was verified end to end on 2026-09-15: the app loads, the frozen schema prompt resolves, and a live NL->SQL question returned the correct per-segment rates for $0.00059 with the guard's LIMIT injected -- which also proves the hardened SnapshotRunner (read_only, enable_external_access=false, lock_configuration, startup self-attack) works on Streamlit's Linux container. dbt_docs_url was verified on 2026-09-16 by request, not by status: curl returned HTTP 200 with ssl_verify_result=0 and 1,818,461 bytes (the dbt docs index), and the presented certificate is CN=sardorbek.codes, issuer Let's Encrypt, SANs sardorbek.codes and www.sardorbek.codes, valid 2026-09-16 to 2026-12-15. It had been held back for 24h while GitHub's Pages API omitted the https_certificate field entirely -- which is NOT a pending state, and is why polling `.https_certificate.state` returned empty rather than a value. Removing and re-adding the custom domain moved it to authorization_pending and then approved within 13 minutes. MIGRATION NOTE, so it is scheduled rather than discovered: this URL depends on sardorbek.codes being the GitHub Pages user site. The planned portfolio site moves the apex to Vercel, at which point this path stops resolving AND the github.io URL stops redirecting to anything useful. When that happens, remove the custom domain from the Pages user site and set this to https://sardorbek-suyunov.github.io/olist-customer-intelligence/, which needs no certificate provisioning because it is covered by the *.github.io wildcard. Verified on 2026-09-16, with the custom domain temporarily removed, that this fallback serves HTTP 200 with the same 1,818,461 bytes and a valid wildcard certificate. Then run `make profile && make readme`."
}
