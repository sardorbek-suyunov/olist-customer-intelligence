{
  "demo_url": "https://olist-customer-intelligence-ssz.streamlit.app",
  "dbt_docs_url": "https://sardorbek-suyunov.github.io/olist-customer-intelligence/",
  "verified": "2026-09-16",
  "provenance": "Set a URL ONLY after it is live and has been opened in a browser, and set `verified` to the date it was checked. README.md previously read 'Live at the URL above' with no URL above it -- a claim with nothing behind it, which this project treats as worse than an absent badge. demo_url was CHANGED on 2026-09-16 when the Streamlit subdomain was renamed from olist-customer-intelligence-zwtfxo5cpowpdzk8ycu4ju to olist-customer-intelligence-ssz. The old subdomain is released on rename and now serves 'You do not have access to this app or it does not exist', so the committed README link was dead until this was updated. HOW TO CHECK THIS URL, because the obvious way silently cannot: Streamlit Community Cloud 303s a cookieless client into a /-/login redirect loop, and does it IDENTICALLY for a live public app and a deleted one -- plain `curl -L` exhausted 50 redirects on both and could not tell them apart, which is a check that returns the same answer whichever is true. WITH a cookie jar it works and is exact: `curl -sSL -c jar -b jar -o /dev/null -w '%{http_code}'` returns 200 for the live app and 404 for the deleted one. Both were also confirmed in a real browser on 2026-09-16: the new URL renders the dashboard with no sign-in, titled 'Olist Customer Intelligence', worked-examples table populated; the old one shows 'You do not have access to this app or it does not exist'. The original end-to-end verification, on 2026-09-15, still stands: the app loads, the frozen schema prompt resolves, and a live NL->SQL question returned the correct per-segment rates for $0.00059 with the guard's LIMIT injected -- which also proves the hardened SnapshotRunner (read_only, enable_external_access=false, lock_configuration, startup self-attack) works on Streamlit's Linux container. dbt_docs_url was verified on 2026-09-16 by request, not by status: curl returned HTTP 200 with ssl_verify_result=0 and 1,818,461 bytes (the dbt docs index), and the presented certificate is CN=sardorbek.codes, issuer Let's Encrypt, SANs sardorbek.codes and www.sardorbek.codes, valid 2026-09-16 to 2026-12-15. It had been held back for 24h while GitHub's Pages API omitted the https_certificate field entirely -- which is NOT a pending state, and is why polling `.https_certificate.state` returned empty rather than a value. Removing and re-adding the custom domain moved it to authorization_pending and then approved within 13 minutes. MIGRATION NOTE, so it is scheduled rather than discovered: this URL depends on sardorbek.codes being the GitHub Pages user site. The planned portfolio site moves the apex to Vercel, at which point this path stops resolving AND the github.io URL stops redirecting to anything useful. When that happens, remove the custom domain from the Pages user site and set this to https://sardorbek-suyunov.github.io/olist-customer-intelligence/, which needs no certificate provisioning because it is covered by the *.github.io wildcard. Verified on 2026-09-16, with the custom domain temporarily removed, that this fallback serves HTTP 200 with the same 1,818,461 bytes and a valid wildcard certificate. Then run `make profile && make readme`. MIGRATION DONE, 2026-09-16: the apex moved to Vercel for the portfolio site, so dbt_docs_url was changed from https://sardorbek.codes/olist-customer-intelligence/ to the github.io address the note above scheduled. The order matters and was followed: this file was changed and the README regenerated BEFORE the custom domain was removed from the Pages user site, because the github.io URL resolves at every point in the sequence -- via GitHub's blanket 301 to the apex while the custom domain was still set, and directly under the *.github.io wildcard certificate afterwards -- whereas the apex path dies the moment the custom domain is removed. Doing it the other way round leaves a window in which this repo's public README links to a 404."
}
